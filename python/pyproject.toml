[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kvcache"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "textual>=0.47.0",
    "matplotlib>=3.7.0",
    "rich>=13.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["kvcache"]
